# Degradation-Invariant Learning

负责人：何青泽

本模块用于研究退化不变的视觉语言表示学习策略。核心目标是在训练或微调阶段约束同一文档页面的干净版本和退化版本生成一致的语义嵌入。

计划内容：

- clean-degraded 正样本对构造
- Patch 嵌入对齐损失
- 对比学习目标
- 风格归一化或域对抗训练模块
- 退化不变表示的验证实验

当前目录先保留模块边界，后续实现应尽量与 `robust/degradation/` 生成的数据集配置解耦，通过显式配置读取样本对。
