# 2026-05-26 何青泽退化不变 Adapter 正式训练与评测总结

## 结论

本报告是 2026-05-25 到 2026-05-26 两天工作的完整整理，覆盖本地 111 页退化子集接入、退化敏感性验证、表示漂移分析、失败方法复盘、标准推理对齐、稳健 Late Interaction 试验，以及最终冻结 ColQwen2 的退化不变 Adapter 训练结果。

本轮已经得到一个可用结果：`tune_distill6` 配置在主 raw degraded 测试集 `PD_MB_GN_JC_LR_CS` 上明显优于原始 ColQwen2 MaxSim。

- 主测试 `nDCG@5`: `0.4506 -> 0.4997`，提升 `+0.0491`。
- 主测试 `Recall@5`: `0.3727 -> 0.4259`，提升 `+0.0532`。
- 主测试 `MRR`: `0.5957 -> 0.6372`，提升 `+0.0414`。
- 该提升超过原先设定的 `nDCG@5 +0.01` 合格线，也超过 `+0.03` 理想线。因此本轮不继续调参，转入整理记录。

补充的 proxy restored 测试中，`gaussian / nlmeans / wiener` 三个 proxy 的 `nDCG@5` 都有正提升；但 5 个额外高强度 raw held-out variant 的平均 `nDCG@5` 略降，说明当前 adapter 对主测试组合有效，对所有高强度组合的泛化仍不稳定。

最重要的科学结论有两点：

1. 退化确实造成了 ColQwen2 文档表示漂移。5 月 25 日的漂移分析显示，同一页面 clean 与 degraded 的 mean-pooled page embedding 平均 cosine 只有 `0.5327`，clean/degraded score matrix 的 Pearson/Spearman 也只有 `0.7622 / 0.7355`，说明退化不只是图像质量下降，也改变了表示空间和排序结构。
2. 最终有效的 Adapter 不是靠简单地把 page-level mean embedding 拉回 clean，而是靠 clean teacher score matrix 和 qrels 排序约束，修正 Late Interaction 层面的检索分数。补充漂移计算显示，Adapter 后主测试 page-level mean cosine 反而从 `0.5327` 降到 `0.3108`，但检索 `nDCG@5` 提升 `+0.0491`。这说明“均值向量更像 clean”不是充分目标，真正应该优化的是多向量 MaxSim 排序行为。

## 开题报告对应关系

开题报告《第八组开题报告V3.pdf》中与何青泽相关的定位是“面向受损文档的视觉语言模型鲁棒性增强”。报告中指出，ColPali / ColQwen2 这类视觉语言模型在高质量数字原生页面上训练较多，面对真实扫描、模糊、噪声、JPEG 压缩、低分辨率、几何偏移等退化时，可能把退化外观错误地纳入多向量表示，导致语义内容与版面结构表示不稳定。

开题报告给何青泽的具体任务包括：

| 开题报告任务 | 本轮对应工作 |
|---|---|
| 基于对比学习的退化不变特征学习 | 5 月 25 日先尝试 clean/degraded page-pair calibration，确认只做 page-level 对齐会损伤检索；5 月 26 日改为 score distillation + qrels ranking 的 residual adapter |
| 让 clean 与 degraded 的 Patch 嵌入更一致 | 增加 `analyze_invariant_features.py` 漂移分析，量化 page-level cosine、L2、score matrix correlation 和 query rank change |
| 考察退化因素与语义内容解耦 | 通过 drift 指标证明退化改变表示；通过 adapter 后 drift 与检索指标的反向现象证明“表示一致性”必须按检索目标定义，不能只看均值向量 |
| 在合成退化数据集上验证检索鲁棒性 | 使用本地 111 页 clean/degraded 对齐子集，覆盖单退化、双退化、三退化、四/五退化组合；主测试为 `PD_MB_GN_JC_LR_CS` |
| 分析计算开销与性能增益权衡 | 比较 multiview、Top-k mean、linear calibration、residual adapter；最终选择冻结主干、只训练 128 维小 adapter 的轻训练方案 |

因此，本轮工作不是偏离开题报告，而是把开题报告中较宽泛的“对比学习、风格归一化、域对抗、Patch 置信度”等方向，收束成当前代码和数据条件下更可复现的一条路线：

```text
冻结 ColQwen2 主干
-> 缓存 clean/degraded/restored page embeddings
-> 分析退化造成的表示漂移和排序漂移
-> 用 clean score matrix 作为 teacher
-> 训练一个小 residual adapter 修正 degraded/restored embedding 的检索分数
```

## 两天工作脉络

### 2026-05-25：先证明问题存在，再淘汰弱方法

5 月 25 日主要做了四件事。

第一，接入本地 111 页退化子集。这个子集对应同一份主文档 `employment_and_social_developments_in_europe_2024-KEBD24002ENN`，同时有 clean 页图和多种 degraded 页图，因此适合做 clean/degraded 严格配对分析。

第二，复现 singleview baseline，确认退化真的会伤害检索。全量 174 query 口径下，clean singleview 到 degraded singleview 的下降约为：

| 指标 | clean singleview | degraded singleview | 退化损失 |
|---|---:|---:|---:|
| nDCG@5 | 0.5728 | 0.4647 | -0.1081 |
| Recall@5 | 0.5750 | 0.4179 | -0.1571 |
| MRR | 0.6636 | 0.6286 | -0.0350 |

第三，做表示漂移分析。结果显示 clean/degraded 同页 mean-pooled embedding 平均 cosine 只有 `0.5327`，并且有页面低到 `-0.0815`。这说明退化不是只降低像素质量，而是把文档页面推离了 clean 表示空间。

第四，尝试并淘汰两个弱方法：

| 方法 | 结果 | 判断 |
|---|---|---|
| weighted multiview | degraded nDCG@5 约 `+0.0051`，MRR 约 `+0.0150`，Recall@5 略降 | 有轻微帮助，但推理成本高，不作为主方法 |
| 第一版 invariant calibration | nDCG@5 `-0.0173`，Recall@5 `-0.0163`，MRR `-0.0121` | 负结果，说明 page-level 对齐目标会破坏 MaxSim 排序结构 |

### 2026-05-26：回到标准推理，再转向轻训练 Adapter

5 月 26 日先清理旧改进，回到标准 singleview MaxSim 作为统一 baseline。随后补了一组免训练稳健 Late Interaction：

| 方法 | nDCG@5 gain | Recall@5 gain | MRR gain | 判断 |
|---|---:|---:|---:|---|
| Top-k mean, k=2 | +0.0022 | -0.0015 | +0.0105 | 弱正 |
| Top-k mean, k=3 | +0.0055 | +0.0042 | +0.0138 | 三项正但幅度小 |
| SmoothMax, tau=0.05 | -0.0083 | -0.0099 | -0.0076 | 负结果 |

这些免训练方法说明“只改 MaxSim 聚合”有一点空间，但提升不足以支撑一个强结论。因此最终切换到轻训练方案：冻结 ColQwen2，只训练一个 128 维 residual adapter，在 embedding 层对 degraded/restored 表示做小幅变换，并直接以 clean 检索分数矩阵和 qrels 排序为监督。

## 数据集与指标口径

### 数据来源

本轮实验没有触碰其他成员研究内容，只使用何青泽方向的本地 111 页退化对齐数据：

```text
documents / data 对应文档:
employment_and_social_developments_in_europe_2024-KEBD24002ENN
page count = 111
```

数据形式包括：

| 类型 | 含义 | 用途 |
|---|---|---|
| clean | 原始干净页图 | teacher embedding、clean baseline、漂移参照 |
| degraded | 同页退化图 | raw degraded baseline、adapter 输入 |
| restored proxy | 用 `nlmeans / gaussian / wiener` 对 degraded 图做传统复原 | 模拟后续其他同学预处理输出的接入口 |

退化代号：

| 代号 | 含义 |
|---|---|
| `PD` | perspective / page distortion 类几何扰动 |
| `MB` | motion blur |
| `GN` | Gaussian noise |
| `JC` | JPEG compression |
| `LR` | low resolution |
| `CS` | color / contrast shift |

主测试 `PD_MB_GN_JC_LR_CS` 是六类退化的组合，训练和验证阶段没有使用它。

### Query split

为了避免 6 种语言翻译造成语义泄漏，query 不按单条随机切分，而是按 `raw_answers` 语义组切分。

| split | 语义组数 | query 数 | 用途 |
|---|---:|---:|---|
| train | 17 | 102 | 训练 adapter |
| val | 6 | 36 | early stopping |
| test | 6 | 36 | 主测试和最终报告 |

注意：5 月 25 日 baseline 和 drift 中有部分结果使用全量 174 query，是为了证明退化影响和分析全局漂移；5 月 26 日 adapter 的正式训练/验证/测试使用 102/36/36 query split，所以两天表格中的绝对数值不能简单逐项相减，应该看各自实验口径下的相对变化。

### Variant split

| split | variant 数 | 说明 |
|---|---:|---|
| train | 30 | 6 单退化 + 15 双退化 + 9 三退化 |
| val | 16 | 剩余三退化 + 5 个四退化 |
| test | 6 | 主测试 `PD_MB_GN_JC_LR_CS` + 5 个额外高强度四/五退化 |

本轮正式训练命令为了先验证方法有效性，实际使用 `--max-variants 6`，即只用 train split 前 6 个单退化 `CS/GN/JC/LR/MB/PD` 训练；val 使用前 6 个 val variants。这是后面泛化不稳的重要原因。

### 指标解释

| 指标 | 易懂解释 | 研究含义 |
|---|---|---|
| nDCG@5 | 前 5 个结果里，相关页排得越靠前越高 | 主指标，评价排序质量 |
| Recall@5 | 所有关联页中，有多少被前 5 个结果命中 | 评价是否找得到相关页 |
| MRR | 第一个相关页越靠前越高 | 评价用户最快看到正确答案的速度 |
| page cosine | 同一页 clean/degraded 均值 embedding 的余弦相似度 | 评价表示漂移 |
| score correlation | clean/degraded query-page 分数矩阵相关性 | 评价排序结构是否整体改变 |
| rank delta | adapter 前后首个相关页排名变化 | 定位哪些 query 被修复或伤害 |

## 代码改动

只改动何青泽退化不变检索实验相关内容，没有改其他成员研究模块。

- `experiments/invariant_splits.py`: 新增按 `raw_answers` 语义组划分 query 的 split 逻辑，并固定 variant split。
- `experiments/build_invariant_splits.py`: 生成 `splits_seed13.json`。
- `experiments/invariant_embeddings.py`: 修复过期 `preprocess_view="identity"` 调用；支持任意退化 variant cache；增加 proxy restored embedding cache。
- `robust/invariant_learning/adapter.py`: 新增 `ResidualEmbeddingAdapter`。
- `robust/invariant_learning/losses.py`: 新增 LateInteraction score distillation、multi-positive ranking loss、identity loss。
- `experiments/run_invariant_adapter_training.py`: 新增训练入口，冻结 ColQwen2，只训练 adapter。
- `experiments/evaluate_invariant_adapter.py`: 新增 checkpoint 评测入口，支持 raw / proxy restored / 多 raw variant。
- `tests/test_invariant_adapter.py`: 新增 split、adapter、loss、cache 单元测试。

## Adapter 数据划分细节

Adapter 训练使用何青泽 focus 文档的 111 页 corpus。query 不随机逐条划分，而是按 `raw_answers` 作为语义 group key，避免 6 种语言翻译泄漏。

| split | 语义组数 | query 数 |
|---|---:|---:|
| train | 17 | 102 |
| val | 6 | 36 |
| test | 6 | 36 |

Variant split manifest:

| split | variant 数 | 说明 |
|---|---:|---|
| train | 30 | 6 单退化 + 15 双退化 + 9 三退化 |
| val | 16 | 剩余三退化 + 5 个四退化 |
| test | 6 | 主测试 `PD_MB_GN_JC_LR_CS` + 5 个额外高强度四/五退化 |

注意：本轮正式调参为了先验证方法有效性，实际训练命令使用 `--max-variants 6`，即只用 train split 前 6 个单退化 `CS/GN/JC/LR/MB/PD` 训练；val 使用前 6 个 val variants。这个选择已经在主测试上有效，但也解释了额外高强度 held-out 泛化仍不稳。

## 表示漂移分析

漂移分析是这两天工作的关键证据链：先证明退化确实改变了 ColQwen2 表示，再解释为什么第一版 calibration 失败，最后说明为什么 score distillation adapter 能成功。

### 5 月 25 日：退化导致 clean/degraded 表示空间偏移

漂移分析来自：

```text
results/invariant_features/feature_drift_summary.json
results/invariant_features/page_level_similarity.csv
results/invariant_features/query_drop_cases.csv
```

主退化 variant 为 `PD_MB_GN_JC_LR_CS`，page count 为 111。

| 漂移指标 | 数值 |
|---|---:|
| mean page cosine | 0.532664843331519 |
| min page cosine | -0.08147381991147995 |
| mean page L2 | 0.8958806430434322 |
| max page L2 | 1.4706963300704956 |
| score matrix Pearson | 0.762178361415863 |
| score matrix Spearman | 0.7354867458343506 |

直观解释：

```text
如果退化只影响人眼观感，而不影响模型语义表示，
同一页 clean/degraded embedding 的 cosine 应该很接近 1。
现在平均只有 0.533，说明模型看到的“同一页”已经明显变成了另一个方向的表示。
```

score matrix correlation 仍有 `0.76 / 0.74`，说明模型并非完全失效；它还保留了一部分语义检索能力。但相关性没有高到接近 1，说明 query-page 排序结构已经被明显扰动。这也解释了为什么 degraded singleview 的 `nDCG@5 / Recall@5` 比 clean 低很多。

query rank change 进一步说明退化影响不是均匀的：

| 类型 | query 数 |
|---|---:|
| degraded 后首个 relevant rank 变差 | 55 |
| degraded 后首个 relevant rank 变好 | 49 |
| 不变 | 70 |

这里的重点不是“有些 query 变好”，而是退化会重新排列 query-page 的相对分数。对部分 query，噪声、模糊或压缩伪影可能削弱了真正相关页；对另一些 query，退化偶然改变了非相关页的相似度。这就是后续必须做 per-query rank delta 的原因。

### 为什么第一版 calibration 失败

5 月 25 日的线性 calibration 用 page-level InfoNCE + cosine alignment 训练，loss 确实下降，但检索指标下降：

| 方法 | nDCG@5 | Recall@5 | MRR |
|---|---:|---:|---:|
| degraded singleview | 0.4647 | 0.4179 | 0.6286 |
| degraded invariant calibration | 0.4473 | 0.4016 | 0.6165 |
| gain | -0.0173 | -0.0163 | -0.0121 |

这说明一个很重要的负结果：

```text
把页面均值 embedding 拉近 clean，不等价于让 ColQwen2 的 MaxSim 检索变好。
```

ColQwen2 的核心不是单个页面向量，而是 query token 和 document token/Patch 之间的 late interaction：

```text
score(q, d) = sum_i max_j <q_i, d_j>
```

如果校准层破坏了 token-level 局部匹配结构，即使 page-level 均值更接近 clean，最终排名也可能变差。因此 5 月 26 日的方法改成直接蒸馏 clean score matrix，并用 qrels 约束排序。

### 5 月 26 日：Adapter 后的漂移现象

为了补充 Adapter 的表示解释，我额外计算了 adapter 前后 page-level mean embedding 与 clean 的 cosine / L2。结果保存于：

```text
artifacts/invariant_adapter/tune_distill6_seed13/adapter_embedding_drift_summary.json
```

主测试 `PD_MB_GN_JC_LR_CS`:

| 指标 | adapter 前 | adapter 后 | 变化 |
|---|---:|---:|---:|
| mean page cosine vs clean | 0.5327 | 0.3108 | -0.2219 |
| min page cosine vs clean | -0.0815 | -0.1171 | -0.0357 |
| mean page L2 vs clean | 0.8959 | 1.1609 | +0.2650 |
| cosine 改善页数 | - | 13 / 111 | - |
| cosine 变差页数 | - | 98 / 111 | - |

三个 proxy restored 也有类似现象：

| 输入 | mean cosine 前 | mean cosine 后 | nDCG@5 gain |
|---|---:|---:|---:|
| gaussian proxy | 0.5568 | 0.3257 | +0.0164 |
| nlmeans proxy | 0.5603 | 0.3440 | +0.0141 |
| wiener proxy | 0.5630 | 0.3395 | +0.0183 |

这个结果乍看反直觉：为什么更不像 clean，检索反而更好？

答案是本轮 Adapter 不是在优化 page-level mean cosine，而是在优化：

1. degraded/restored score distribution 接近 clean teacher score distribution；
2. qrels positive pages 在当前 query 下排得更靠前；
3. adapter 不要过度破坏原始 token embedding。

也就是说，Adapter 学到的不是“全局均值回到 clean 方向”，而是“在 query-token 与 document-token 的 MaxSim 结构里，让相关页的分数关系更接近 clean”。这正好回应了 5 月 25 日 calibration 的失败原因：退化不变特征学习不能只看单页均值，要看检索分数矩阵和排序。

### 漂移分析对泛化结果的解释

额外 high-strength raw held-out variant 中，`PD_GN_JC_LR_CS` 原始 mean cosine 已经非常高：

| variant | mean cosine 前 | mean cosine 后 | nDCG@5 gain |
|---|---:|---:|---:|
| `PD_GN_JC_LR_CS` | 0.9108 | 0.4713 | -0.0426 |

这说明某些未训练退化组合本身已经离 clean 很近，强行套用只在 6 个单退化上训练出的 adapter，反而会把表示推离原来的良好区域，导致排序变差。这解释了为什么额外 5 个 raw held-out variant 的平均 nDCG@5 是负的，也提示下一轮必须把训练 variant 扩展到更复杂的组合，而不是继续只用单退化训练。

## 方法

ColQwen2 主干全程冻结，只训练 embedding 层 residual adapter。

Adapter:

```text
LayerNorm(128) -> Linear(128, 256) -> GELU -> Linear(256, 128)
output = normalize(x + clamp(scale, 0, 0.5) * adapter(x))
```

Loss:

```text
L = score_weight * L_score_distill
  + rank_weight * L_qrels_rank
  + identity_weight * L_identity
```

本轮最佳配置为 `tune_distill6`:

```text
score_weight=2.0
rank_weight=0.5
identity_weight=0.01
temperature=0.07
lr=1e-3
epochs=30
patience=6
best_epoch=2
```

## 运行记录

训练按要求使用 `conda activate colqwen2-test`，并用 tmux 隔离不同流程。三组参数分别跑在 CUDA 1/2/3。

```bash
tmux new-session -d -s inv_adapter_default6_cuda1 'cd <repo> && source <conda>/etc/profile.d/conda.sh && conda activate colqwen2-test && python experiments/run_invariant_adapter_training.py ... --device cuda:1 --max-variants 6 --output-dir results/invariant_learning/tune_default6'

tmux new-session -d -s inv_adapter_rank6_cuda2 'cd <repo> && source <conda>/etc/profile.d/conda.sh && conda activate colqwen2-test && python experiments/run_invariant_adapter_training.py ... --device cuda:2 --score-weight 0.5 --rank-weight 1.5 --identity-weight 0.02 --max-variants 6 --output-dir results/invariant_learning/tune_rank6'

tmux new-session -d -s inv_adapter_distill6_cuda3 'cd <repo> && source <conda>/etc/profile.d/conda.sh && conda activate colqwen2-test && python experiments/run_invariant_adapter_training.py ... --device cuda:3 --score-weight 2.0 --rank-weight 0.5 --identity-weight 0.01 --max-variants 6 --output-dir results/invariant_learning/tune_distill6'
```

补充评测:

```bash
tmux new-session -d -s inv_adapter_eval_proxies_cuda1 'cd <repo> && source <conda>/etc/profile.d/conda.sh && conda activate colqwen2-test && python experiments/evaluate_invariant_adapter.py --checkpoint artifacts/invariant_adapter/tune_distill6_seed13/adapter_checkpoint.pt --device cuda:1 --restored-proxies nlmeans gaussian wiener --output-dir results/invariant_learning/tune_distill6_eval_all_proxy --cache-root /tmp/invariant_adapter_cache_distill6'

tmux new-session -d -s inv_adapter_eval_extra_raw_cuda2 'cd <repo> && source <conda>/etc/profile.d/conda.sh && conda activate colqwen2-test && python experiments/evaluate_invariant_adapter.py --checkpoint artifacts/invariant_adapter/tune_distill6_seed13/adapter_checkpoint.pt --device cuda:2 --eval-raw-variants MB_GN_JC_LR_CS PD_MB_GN_JC_LR PD_MB_GN_JC_CS PD_MB_GN_LR_CS PD_GN_JC_LR_CS --skip-restored-proxies --output-dir results/invariant_learning/tune_distill6_eval_extra_raw --cache-root /tmp/invariant_adapter_cache_distill6'
```

## 参数对比

主测试集均为 `test query groups x PD_MB_GN_JC_LR_CS`。

| 配置 | loss 权重 | best val nDCG@5 | raw original | adapter | nDCG@5 gain |
|---|---|---:|---:|---:|---:|
| `tune_default6` | `1.0 / 0.5 / 0.05` | 0.5652 | 0.4506 | 0.4713 | +0.0207 |
| `tune_rank6` | `0.5 / 1.5 / 0.02` | 0.5579 | 0.4506 | 0.4593 | +0.0087 |
| `tune_distill6` | `2.0 / 0.5 / 0.01` | 0.5569 | 0.4506 | 0.4997 | +0.0491 |

`tune_distill6` 的验证 nDCG 不是最高，但主测试提升最大；当前任务目标是主 raw degraded 测试提升，所以采用该 checkpoint。

## 主测试结果

`PD_MB_GN_JC_LR_CS`，36 test queries，111 pages。

| 方法 | nDCG@5 | Recall@5 | MRR |
|---|---:|---:|---:|
| clean original MaxSim | 0.6653 | 0.6435 | 0.7301 |
| degraded original MaxSim | 0.4506 | 0.3727 | 0.5957 |
| degraded Top-k mean baseline | 0.4740 | - | - |
| degraded + trained adapter | 0.4997 | 0.4259 | 0.6372 |
| adapter gain vs degraded original | +0.0491 | +0.0532 | +0.0414 |

Rank delta: `improved=11, worsened=3, unchanged=22`。

## Proxy Restored 结果

仍然是 `PD_MB_GN_JC_LR_CS`，只替换输入为 proxy restored page image 后重新编码。

| proxy | original nDCG@5 | Top-k nDCG@5 | adapter nDCG@5 | nDCG@5 gain | Recall@5 gain | MRR gain |
|---|---:|---:|---:|---:|---:|---:|
| gaussian | 0.4332 | 0.4481 | 0.4496 | +0.0164 | +0.0116 | +0.0197 |
| nlmeans | 0.4513 | 0.4758 | 0.4654 | +0.0141 | +0.0556 | -0.0411 |
| wiener | 0.4781 | 0.4650 | 0.4964 | +0.0183 | +0.0417 | +0.0065 |

结论：proxy restored 的 `nDCG@5` 全部为正提升；`nlmeans` 的 MRR 有下降，说明 adapter 对早排位的影响仍不完全稳定。

## 额外 Raw Held-out 泛化结果

这些 variant 都没有进入训练和验证。结果说明：当前 adapter 对主测试组合很有效，但并非对所有高强度组合都稳定泛化。

| variant | original nDCG@5 | adapter nDCG@5 | nDCG@5 gain | Recall@5 gain | MRR gain |
|---|---:|---:|---:|---:|---:|
| `MB_GN_JC_LR_CS` | 0.2976 | 0.2879 | -0.0097 | +0.0000 | -0.0363 |
| `PD_MB_GN_JC_LR` | 0.4270 | 0.4189 | -0.0080 | -0.0069 | +0.0664 |
| `PD_MB_GN_JC_CS` | 0.5849 | 0.5708 | -0.0140 | -0.0648 | +0.0343 |
| `PD_MB_GN_LR_CS` | 0.5202 | 0.5485 | +0.0283 | +0.0208 | +0.0202 |
| `PD_GN_JC_LR_CS` | 0.6505 | 0.6079 | -0.0426 | -0.0718 | -0.0230 |

额外 5 个 raw held-out 平均:

```text
original nDCG@5 = 0.4960
adapter nDCG@5  = 0.4868
mean gain       = -0.0092
positive count  = 1 / 5
```

因此不能宣称“所有未见高强度退化组合都提升”。当前可以宣称的是：在报告主测试 `PD_MB_GN_JC_LR_CS` 和三个 proxy restored 上有明确提升。

## 验证

运行检查:

```text
python -m compileall experiments robust tests
conda run -n colqwen2-test python -m pytest tests/test_invariant_adapter.py tests/test_robust_late_interaction.py tests/test_run_local_hr_benchmark.py tests/test_metrics.py -q
```

结果:

```text
17 passed in 5.49s
```

日志检查:

```text
Traceback / RuntimeError / CUDA out / Exception: 未发现
tmux: 所有训练和补充评测会话均已结束
```

## 产物路径

最佳 checkpoint:

```text
artifacts/invariant_adapter/tune_distill6_seed13/adapter_checkpoint.pt
```

主要结果:

```text
artifacts/invariant_adapter/tune_distill6_seed13/README.md
artifacts/invariant_adapter/tune_distill6_seed13/metadata.json
artifacts/invariant_adapter/tune_distill6_seed13/adapter_embedding_drift_summary.json
results/invariant_learning/tune_distill6/test_raw_PD_MB_GN_JC_LR_CS.json
results/invariant_learning/tune_distill6_eval_all_proxy/eval_restored_proxy_gaussian.json
results/invariant_learning/tune_distill6_eval_all_proxy/eval_restored_proxy_nlmeans.json
results/invariant_learning/tune_distill6_eval_all_proxy/eval_restored_proxy_wiener.json
results/invariant_learning/tune_distill6_eval_extra_raw/*.json
```

Split manifest:

```text
results/invariant_learning/tune_distill6/splits_seed13.json
```

## 后续建议

短期可以把 `tune_distill6` 作为当前阶段的正式方法结果写入研究记录，因为主测试提升已经足够明显。

如果后续要追求更强泛化，建议下一轮不要继续围绕当前 6 个单退化微调，而是改成 hard variant 训练，例如：

```text
--variant-order hard --max-variants 12
--variant-order hard --max-variants 18
--max-variants 30
```

核心目标是让训练分布覆盖更多三退化/双退化组合，而不是继续加 epoch。当前早停 best epoch 为 2，说明不是训练轮数不足，而是训练 variant 覆盖不足。

## 2026-05-27 补充：完整 train split 复跑

根据后续复查，上一版 `tune_distill6` 的正式 checkpoint 实际只用 `--max-variants 6`，也就是 `CS/GN/JC/LR/MB/PD` 六个单退化 variant 训练。这个设置能证明方法有效，但训练分布偏窄，确实不能称为“完整训练集训练”。

因此在 `upstream/main` 更新后，新建分支 `heqingze-invariant-adapter-fulltrain`，重新运行了一版完整 train split adapter。此次复跑没有传 `--max-variants`，训练覆盖 manifest 中全部 30 个 train variants：

```text
6 单退化:
CS, GN, JC, LR, MB, PD

15 双退化:
GN_CS, GN_JC, GN_LR, JC_CS, JC_LR, LR_CS,
MB_CS, MB_GN, MB_JC, MB_LR,
PD_CS, PD_GN, PD_JC, PD_LR, PD_MB

9 三退化:
MB_GN_LR, MB_JC_CS, PD_JC_LR, PD_MB_GN, PD_LR_CS,
PD_MB_JC, PD_JC_CS, MB_LR_CS, PD_MB_LR
```

Query split 仍保持不变：

| split | 语义组数 | query 数 |
|---|---:|---:|
| train | 17 | 102 |
| val | 6 | 36 |
| test | 6 | 36 |

运行命令核心参数：

```bash
tmux new-session -d -s inv_adapter_fulltrain_cuda1 \
  'cd /home/qz/projects/colpali-segmentation &&
   source /home/qz/miniconda3/etc/profile.d/conda.sh &&
   conda activate colqwen2-test &&
   export CUDA_VISIBLE_DEVICES=1 &&
   python experiments/run_invariant_adapter_training.py \
     --model ./colqwen2-v1.0 \
     --local-files-only \
     --device cuda:0 \
     --epochs 30 \
     --patience 6 \
     --score-weight 2.0 \
     --rank-weight 0.5 \
     --identity-weight 0.01 \
     --temperature 0.07 \
     --lr 1e-3 \
     --restored-proxies nlmeans gaussian wiener \
     --cache-root results/invariant_cache \
     --output-dir results/invariant_learning/full_train_seed13'
```

训练日志显示 best epoch 为 1，随后验证集持续下降，early stopping 在 epoch 7 后结束：

| epoch | train loss | val nDCG@5 |
|---:|---:|---:|
| 1 | 0.8036 | 0.4598 |
| 2 | 0.5515 | 0.4509 |
| 3 | 0.4643 | 0.4015 |
| 4 | 0.4690 | 0.3699 |
| 5 | 0.4024 | 0.3646 |
| 6 | 0.3135 | 0.3237 |
| 7 | 0.2695 | 0.2780 |

主 raw test `PD_MB_GN_JC_LR_CS` 结果：

| 方法 | nDCG@5 | Recall@5 | MRR |
|---|---:|---:|---:|
| degraded original MaxSim | 0.4506 | 0.3727 | 0.5957 |
| full-train adapter | 0.4784 | 0.3912 | 0.6174 |
| gain | +0.0278 | +0.0185 | +0.0217 |

Rank delta: `improved=10, worsened=8, unchanged=18`。

Proxy restored 结果：

| proxy | original nDCG@5 | adapter nDCG@5 | nDCG@5 gain | Recall@5 gain | MRR gain |
|---|---:|---:|---:|---:|---:|
| gaussian | 0.4332 | 0.4024 | -0.0308 | -0.0648 | +0.0087 |
| nlmeans | 0.4513 | 0.4745 | +0.0232 | +0.0046 | +0.0020 |
| wiener | 0.4781 | 0.4698 | -0.0083 | -0.0185 | -0.0031 |

结论：

1. 完整 train split checkpoint 相比 degraded original 仍然有主测试正收益，说明扩大训练 variant 覆盖后方法没有失效。
2. 但它没有超过上一版 `tune_distill6` 六单退化 checkpoint。主测试 nDCG@5 从上一版的 `0.4997` 下降到 `0.4784`。
3. Proxy restored 泛化也更不稳定，只有 `nlmeans` 为正，`gaussian / wiener` 为负。
4. 因此，完整训练集复跑适合作为“训练覆盖更完整”的权重和消融结果，但当前最强主测试 checkpoint 仍是上一版 `tune_distill6_seed13`。

新增完整训练权重：

```text
artifacts/invariant_adapter/full_train_seed13/adapter_checkpoint.pt
artifacts/invariant_adapter/full_train_seed13/metadata.json
```

对应结果文件：

```text
results/invariant_learning/full_train_seed13/train_log.json
results/invariant_learning/full_train_seed13/val_metrics_by_epoch.json
results/invariant_learning/full_train_seed13/test_raw_PD_MB_GN_JC_LR_CS.json
results/invariant_learning/full_train_seed13/test_restored_proxy_gaussian.json
results/invariant_learning/full_train_seed13/test_restored_proxy_nlmeans.json
results/invariant_learning/full_train_seed13/test_restored_proxy_wiener.json
```
